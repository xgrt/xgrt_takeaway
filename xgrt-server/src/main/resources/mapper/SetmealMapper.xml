<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xgrt.mapper.SetmealMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        VALUES
        (#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.xgrt.vo.SetmealVO">
        select c.`name` as categoryName,sm.* from setmeal sm left outer join category c on c.id=sm.category_id
        <where>
            <if test="name != null">
                and sm.name like concat("%",#{name},"%")
            </if>
            <if test="categoryId != null">
                and sm.category_id = #{categoryId}
            </if>
            <if test="name != null">
                and sm.status = #{status}
            </if>
        </where>
        order by sm.create_time desc
    </select>
</mapper>
