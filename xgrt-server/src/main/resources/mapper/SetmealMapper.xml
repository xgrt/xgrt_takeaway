<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xgrt.mapper.SetmealMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        VALUES
        (#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.xgrt.vo.SetmealVO">
        select c.`name` as categoryName,sm.* from setmeal sm left outer join category c on c.id=sm.category_id
        <where>
            <if test="name != null">
                and sm.name like concat("%",#{name},"%")
            </if>
            <if test="categoryId != null">
                and sm.category_id = #{categoryId}
            </if>
            <if test="name != null">
                and sm.status = #{status}
            </if>
        </where>
        order by sm.create_time desc
    </select>

    <update id="update">
        update setmeal
        <set>
            <if test="name!=null">
                name = #{name},
            </if>
            <if test="categoryId!=null">
                category_id = #{categoryId},
            </if>
            <if test="price!=null">
                price = #{price},
            </if>
            <if test="status!=null">
                status = #{status},
            </if>
            <if test="description!=null">
                description = #{description},
            </if>
            <if test="image!=null">
                image = #{image},
            </if>
            <if test="updateUser!=null">
                update_user = #{updateUser},
            </if>
            <if test="updateTime!=null">
                update_time = #{updateTime},
            </if>
        </set>
        where id=#{id}
    </update>

    <update id="updateBatch">
        update setmeal
        <set>
            <if test="modelSetmeal.categoryId!=null">
                category_id = #{modelSetmeal.categoryId},
            </if>
            <if test="modelSetmeal.price!=null">
                price = #{modelSetmeal.price},
            </if>
            <if test="modelSetmeal.status!=null">
                status = #{modelSetmeal.status},
            </if>
            <if test="modelSetmeal.description!=null">
                description = #{modelSetmeal.description},
            </if>
            <if test="modelSetmeal.image!=null">
                image = #{modelSetmeal.image},
            </if>
            <if test="modelSetmeal.updateUser!=null">
                update_user = #{modelSetmeal.updateUser},
            </if>
            <if test="modelSetmeal.updateTime!=null">
                update_time = #{modelSetmeal.updateTime},
            </if>
        </set>
        where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>

    <delete id="deleteBatch">
        delete from setmeal where id in
        <foreach collection="ids" separator="," open="(" close=")" item="id">
            #{id}
        </foreach>
    </delete>
</mapper>
